# Shared

Shared modules are grouped by purpose:

- `Config/`: gameplay constants and templates.
- `Contracts/`: schemas and remote contracts.
- `Content/`: map/content manifests.

These folders are the canonical implementation homes.
Legacy root modules remain only as compatibility shims.
