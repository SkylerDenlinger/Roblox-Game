# Social

Owns player-to-player social systems that are not mode-specific.

- `PartyService.lua`: party membership, invites, and party remotes.

Legacy files under `server/Services/` are compatibility shims only.
