# Matchmaking Adapters

Backend adapters for queue/session orchestration.

- queue backends
- session transport backends
- persistence backends

These are the canonical implementations used by `server/Matchmaking`.
