# Config

Server-facing accessors for shared configuration and content manifests.

- `GameConfigService.lua`: feature flags, round rules, queue bands, templates, maps, and remote schemas.

Legacy files under `server/Services/` are compatibility shims only.
